#ifndef GET_STRING_H
#define GET_STRING_H

char isValid(const char *s, char *p, char fisrt, char second);
char makeMessage(char* jsonData, char* message);
void convertToFahrenheit(char *in );
void convertToHumidity(char *in);

#endif